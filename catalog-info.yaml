# Backstage catalog entry, read by paved's scorecard plugin.
#
# Every check paved scores is derived from this file alone — no cluster
# access, no CI credentials. What is absent here is absent from the score.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: chaos-toolbox
  description: >-
      stress-ng + tc/netem chaos experiments in one small image — CPU, memory and I/O pressure, latency,
      packet loss and bandwidth limits — for lightweight resilience testing without installing a full…
  annotations:
    github.com/project-slug: fabiocicerchia/chaos-toolbox
    backstage.io/source-location: url:https://github.com/fabiocicerchia/chaos-toolbox/
    backstage.io/techdocs-ref: dir:.
  links:
    - url: https://github.com/fabiocicerchia/chaos-toolbox
      title: Source
spec:
  type: library
  lifecycle: production
  owner: group:default/kodebeat
  system: oss-tools
